# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'ReactorKitExample' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
  pod 'ReactorKit'
  pod 'RxSwift', '~> 4.0.0'
  pod 'RxCocoa', '~> 4.0.0'

  # Pods for ReactorKitExample

  target 'ReactorKitExampleTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'ReactorKitExampleUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
